* Nhân sự: Kenny
* Việc đã thực hiện:  
	- hoàn thành api delete admin (community channel)
	- hoàn thành api check verify type (bước 1.5 forgot password)\
	- làm api verify TOTP (bước 3 forgot password)
	- sửa api Add Admin (community channel)
	- làm api lấy thông tin channel từ link tham gia 
* Việc hôm nay: 
	* (off)
- Khó khăn: không
---
queryOption.SetOnlyFields("_id", "verify", "new_rule", "deleted_at")  
user, err := queries.NewUser(ctx.Context()).GetByUsername(requestBody.Username, queryOption)  
if err != nil {  
    return err  
}  
if !user.NotBlock {  
    if user.DeletedAt != nil {  
       return response.NewError(constants.ReturnCode616BlockByAdminDelete, response.ErrorOptions{Data: "Forbidden! This user is block by admin"})  
    } else {  
       return response.NewError(constants.ReturnCode604BlockByDisable, response.ErrorOptions{Data: "Forbidden! This user is disable by admin"})  
    }  
}