- **Nhân sự**: *Kenny*
* **Việc đã thực hiện**:    	
	- Dump dữ liệu test secure task (100%)
	- API cập nhật priority (100%)
	- Hoàn thành event xóa project 
	- Check bug upload secure drive 
* **Việc hôm nay**:
	- Làm api, event Signet Task V2
- **Khó khăn**: 
---
#dr
**Feature**
   - [ ] FEAT: sửa frontend
---
#articles
- [ ] Everyone knows your location: tracking myself down through in-app ads
#tech 
- [ ] Code Report: Meta’s Llama 4 is mindblowing… but did it cheat?
#news 
- [x] TP HCM sẽ dùng trụ sở dôi dư sau sáp nhập làm bệnh viện, trường học
---
#nvim #lua 
- [ ] refactor init.lua
	- [ ] 1. Move Mason & LSP setup to lua/configs/lspconfig.lua  
	- [ ] 2. Move Go autocmd (gofmt) into lua/plugins/go.lua or configs  
	- [ ] 3. Use lua/plugins/init.lua to list plugin specs, importing submodules  
	- [ ] 4. Keep init.lua minimal: only bootstrap and require core modules.
---
#mongo 
- [x] bulk.find.delete in Golang
      ```
```
const BATCH_SIZE = 1000;
const projectId = ObjectId("6805f2105a4ff4e216a89269");

while (true) {
  const bulk = db.tasks_test.initializeOrderedBulkOp();

  // stage up to BATCH_SIZE deletes
  bulk
    .find({ project_id: projectId })
    .limit(BATCH_SIZE)
    .remove();            // remove() deletes all docs matched within the limit

  const res = bulk.execute();
  if (res.nRemoved === 0) {
    print("Done.");
    break;
  }
  print(`Removed ${res.nRemoved} docs…`);
}

```
      
- [ ] config proxy
- [ ] check **$convert**
      https://www.mongodb.com/docs/manual/reference/operator/aggregation/convert/
```
db.categories.find({ priority: { $exists: false } }).forEach(item => {  db.categories.updateOne({ _id: item._id }, [  {  $set: {  priority: { $convert: { input: item.created_at.valueOf(), to: "long" } }  }  }  ])  })
``` 
---

#sync #obsidian #dotfiles
- [ ] push obsidian repo  
      https://github.com/trankhanh040147/my-vault
- [ ] add .bashrc
	- [ ] alias
---
- [ ] 5 BASIC DEFENDING SECRETS - How to improve as a defender in soccer FAST
