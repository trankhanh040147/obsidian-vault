- [x] Set timeout connect db ngắn
- [x] api: Compare indexes by database
- [x] api: sync indexes by database
- [x] Show lỗi cụ thể
	- [x] Khi đồng bộ indexes
	- [x] Khi lấy danh sách indexes từ client
- [x] thêm option kiểm tra kết nối database sau khi tạo database 
- [x] Dùng goroutine và wait group để tối ưu sync indexes by database 
- [x] Dump data và test thời gian tạo/gỡ index:
	- [ ] trường hợp ko dùng goroutine, waitGroup
		- [ ] 17.03 s | 17.15 s | 16.45s
	- [ ] dùng goroutine, waitGroup
		- [ ] 13.06 s | 12.25 s | 13.11 s

- [ ] chức năng: xóa collection (admin) --> xóa các indexes thuộc collection đó

#react
- [ ] 

- [ ] Tối ưu performance
	- [ ] Caching difference indexes
	- [ ] Dùng Mutex Lock để handle errors khi dùng goroutines 
	- [ ] Dùng database pooling để reuse connections: Ensure that you are utilizing a connection pool for your database connections. Connection pooling allows for the reuse of established connections, reducing the overhead of creating new connections for each synchronization operation
- [ ] Trường hợp user tạo một db mới, cần sync danh sách indexes từ client --> admin ?
- [ ] Xử lý case user tạo collection ở phía admin
- [ ] Xóa collection phía admin
- [ ] Refactor: parse trực tiếp các primitive ID từ path (không cần dùng ObjectIDFromHex)
- [ ] edge cases:
	- [ ] 1
		- admin: 
			- {name: 1}
			- {name: 1} {unique}
		- client: 
			- {name:1}
		--> sync: duplicated name  ?
			error: "(IndexKeySpecsConflict) Index build failed: ed9cd8d0-ad1c-495c-a73d-fe9e6851c96c: Collection books.readers ( 642b3781-ece5-4d81-9772-ebe4f5c8f994 ) :: caused by :: An existing index has the same name as the requested index. When index names are not specified, they are auto generated and can cause conflicts. Please refer to our documentation. Requested index: { v: 2, key: { name: 1 }, name: \"name_1\" }, existing index: { v: 2, unique: true, key: { name: 1 }, name: \"name_1\" }"
- [ ] Self refactor: 
	- [ ] Check status các lỗi
		- [ ] Check shorthand if *err*
	- [ ] ...
- [ ] Hiển thị thông tin Connection dạng embed (URI, dbName)
---
#mach
- [ ] [tmux] fix MouseDragEnd1Pane
---
**Đã làm**
- api: So sánh indexes giữa db client và db admin theo collection --> cho ra danh sách missingIndexes, matchedIndexes, extraIndexes
- update api SyncIndexesByCollection: lấy kết quả missingIndexes và extraIndexes khi compare để đồng bộ index client
- adapt với ExpiresAt restriction: "[Compound indexes] do not support TTL and ignore the `expireAfterSeconds` option"
- validate: _id_ default index, unique (keys --> keys + options), expires at >= 0 

**Đã làm**
- Thêm option kiểm tra kết nối database khi tạo database (connect & save)
- viết api so sánh indexes admin-client theo database 
- update api đồng bộ indexes client theo database theo kết quả so sánh
- Dùng goroutine và wait group để tối ưu sync indexes by database 
- Dump data và test thời gian tạo/gỡ index:
**Khó khăn**
- Cần research patterns concurrency sử dụng wait groups và go routines
- Api sync indexes tốn thời gian để đợi trả về response
**Kế hoạch**
- Review code và chuẩn bị data demo
- Research Web Socket để implement sync indexes 
