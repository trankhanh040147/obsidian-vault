- [x] Connection --> Database
	- [x] Refactor database --> db, collection --> coll
- [x] API: Connect và lấy danh sách collections từ bất kỳ database trong config 
- [x] API: Sync indexes by DB
- [x] API: Sync indexes by Collection

- [ ] sync indexes by coll:
	- [x] Remove indexes from redundant list
		- [x] Remove dựa vào name của index trước đó 
	- [x] Add indexes from new missing list
		- [x] EC: trường hợp ko thể add missing index do client có một index khác (matched) bị trùng tên --> Set name của index = nil (để hệ thống tự sinh name của index đó)
		- [ ] EC: trường hợp nhập colleciton_name khác với collection name trong list indexes --> collection_name trong indexes không quan trọng 
	
- [ ] API: Compare indexes by collection
	- [x] Get client's indexes
	- [x] Output:
		- [x] Missing indexes
		- [x] Redundant indexes
		- [x] Matched indexes
	- [x] Hiển thị collection name
	- [x] Ẩn database_id, index_id, created_at, updated_at do indexes từ db của client không có các thông tin này
	- [x] Phương pháp check:
		- matched: trùng keys, trùng options
		- missing: có trong list admin, nhưng miss trong list client
		- redundant: có trong list client, nhưng ko có trong list admin
	- ==[ ] Edge cases:==
		- [x] index_id: Compare với một collection thiếu _id_
		- [x] Compare với một collection ko có ở phía client 
		- [ ] Compare với một collection không có ở phía admin --> cần thông báo xóa collection này hay không ?
		- [x] Có một index trùng key, trùng options nhưng khác tên
		- [x] cặp index matched, khác tên và có thêm index khác trùng tên với index của client
		- ==[ ] {book:1; author:-1} vs {author:-1; book:1}} ?==

- [ ] Set timeout connect db ngắn
- [ ] API: Compare indexes by database
- [ ] Phân biệt hoa thường
	- [ ] ?
- [ ] Refactor: parse trực tiếp các primitive ID từ path (không cần dùng ObjectIDFromHex)
- [ ] Hiển thị thông tin Connection dạng embed (URI, dbName)
- [ ] Self refactor: 
	- [ ] Check status các lỗi d
		- [ ] Check shorthand if *err*
	- [ ] ...
---
#mach
- [ ] [tmux] fix MouseDragEnd1Pane
---
**Đã làm**
- api: So sánh indexes giữa db client và db admin theo collection --> cho ra danh sách missingIndexes, matchedIndexes, extraIndexes
- update api SyncIndexesByCollection: lấy kết quả missingIndexes và extraIndexes khi compare để đồng bộ index client
- adapt với ExpiresAt restriction: "[Compound indexes] do not support TTL and ignore the `expireAfterSeconds` option"
- validate: _id_ default index, unique (keys --> keys + options), expires at >= 0 
**Khó khăn**
- thiếu option để kiểm tra connection khi tạo mới 
- response chưa bắt lỗi cụ thể để báo về client khi sync indexes 
**Kế hoạch**
- thêm option kiểm tra kết nối database sau khi tạo database 
- viết api so sánh indexes admin-client theo database 
- update api đồng bộ indexes client theo database 