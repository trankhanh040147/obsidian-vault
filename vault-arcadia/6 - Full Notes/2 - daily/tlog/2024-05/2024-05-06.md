- [x] Connection --> Database
	- [x] Refactor database --> db, collection --> coll
- [x] API: Connect và lấy danh sách collections từ bất kỳ database trong config 
- [x] API: Sync indexes by DB
- [x] API: Sync indexes by Collection


- [ ] Sửa flow sync indexes:
	- [ ] Remove indexes from redundant list
		- [ ] Remove dựa vào name của index trước đó 
	- [ ] Add indexes from new missing list
		- [ ] EC: trường hợp ko thể add missing index do client có một index khác (matched) bị trùng tên

	
- ==[ ] API: Compare indexes by collection==
	- [x] Get client's indexes
	- [x] Output:
		- [x] Missing indexes
		- [x] Redundant indexes
		- [x] Matched indexes
	- [x] Hiển thị collection name
	- [x] Ẩn database_id, index_id, created_at, updated_at do indexes từ db của client không có các thông tin này
	- [x] Phương pháp check:
		- matched: trùng keys, trùng options
		- missing: có trong list admin, nhưng miss trong list client
		- redundant: có trong list client, nhưng ko có trong list admin
	- [ ] Edge cases:
		- [ ] index_id: Compare với một collection thiếu _id_
		- [ ] Compare với một collection ko có ở phía client 
		- [ ] Compare với một collection không có ở phía admin --> cần thông báo xóa collection này hay không ?
		- [ ] Có một index trùng key, trùng options nhưng khác tên
		- [ ] cặp index matched, khác tên và có thêm index khác trùng tên với index của client
		- [ ] 
- [ ] API: Compare indexes by database

- [ ] Refactor: parse trực tiếp các primitive ID từ path (không cần dùng ObjectIDFromHex)
- [ ] Hiển thị thông tin Connection dạng embed (URI, dbName)
- [ ] Self refactor: 
	- [ ] Check status các lỗi d
		- [ ] Check shorthand if *err*
	- [ ] ...
---
#mach
- [ ] [tmux] fix MouseDragEnd1Pane
---
**Đã làm**
- Xử lý case chỉ cho phép các key khác biệt nhau (về field name) khi tạo/sửa indexes
- Xóa các indexes thuộc về connections sau khi xóa một hoặc nhiều connection (dùng goroutine)
- API: Connect và lấy danh sách collections từ bất kỳ database phía admin (filter theo name và có paging)
- Refactor module Connection --> Database
- API: Đồng bộ danh sách indexes sang client theo collection 
- API: Đồng bộ danh sách indexes sang client theo  database
**Khó khăn**
- Cần research cách sử dụng pipeline để: liệt kê distinct, group documents, skip, limit, filtering,  sorting, counting
**Kế hoạch**
- api: Kiểm tra sự khác biệt giữa indexes admin và client theo collection
- api: Kiểm tra sự khác biệt giữa indexes admin và client theo database