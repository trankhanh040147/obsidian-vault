- [ ] Implement authen middleware để protect router
- [ ] Implement usecase: login trả về access token và refresh token nếu đăng nhập đúng thông tin
- [ ] Research: Revoke token, token rotation, reuse detection
- [ ] Tạo model AuthToken để lưu trữ Access Token và Refresh Token 
Obstacles
- Chưa thiết kế db để lưu trữ JWT Token --> Không thể thực hiện Token Rotation
- Chưa có middleware để recover khi service bị panic
- Chưa custom được middleware authorize bằng package gofiber/contrib/jwt
Plan:
- Sửa lại usecase Login:  Extract & tạo access token, refresh token lưuvào DB sau khi login
- Hoàn thiện use case: Refresh để lấy Access Token và Refresh Token mới, áp dụng cơ chế Revoke Token để vô hiệu hóa Refresh Token đã sử dụng (Reuse Detection) 
- Research cách MongoDB lưu dữ liệu index của collection