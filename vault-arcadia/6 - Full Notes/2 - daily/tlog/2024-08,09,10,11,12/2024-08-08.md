- Nhân sự: Kenny
* Việc đã thực hiện:  
	- init project sigpro-mapp-eevent
* Việc hôm nay: 
	 - Làm UI tính năng import indexes 
	- (chờ task mới)
- Khó khăn: không
---  
Update status
- Testing (Dev) --> Beta (Stg) --> Request Review (Stg)

--- 

Drmanager - Import Indexes

<button href="#"  
        onClick={() => onClickImport(data)}  
        className="btn btn-sm btn-success import-btn">  
    <i class="ri-export label-icon align-middle fs-16 me-2"></i>  
    Import  
</button>

const onClickImport = (database) => {  
    // open choose file modal  
  
    // after chose file, call api ImportIndexes    // dispatch(onImportIndexes({database_id: database.id}))};


~~
I writing this in ReactJS, can you help me handle the the file select and get the file to send api ?